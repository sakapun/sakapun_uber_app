projects:
  sakapunuberapp:
    schemaPath: src/graphql/schema.json
    includes:
      - src/graphql/**/*.ts
    excludes:
      - ./amplify/**
    extensions:
      amplify:
        codeGenTarget: typescript
        generatedFileName: types/amplify_api.ts
        docsFilePath: src/graphql
extensions:
  amplify:
    version: 3
